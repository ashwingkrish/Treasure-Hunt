<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_question"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.ashwingk.treasurehunt.QuestionActivity">
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:weightSum="2"
        android:id="@+id/ll_question_timers"
        android:orientation="horizontal">
        <TextView
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:background="@drawable/timer_bg"
            android:textColor="#ffffff"
            android:id="@+id/tv_question_timer"
            android:gravity="center"
            android:padding="4dp"
            android:text="Time Remaining = 3:00"/>
        <TextView
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:background="@drawable/timer_bg"
            android:textColor="#ffffff"
            android:id="@+id/tv_question_answered"
            android:gravity="center"
            android:padding="4dp"
            android:text="Answered: 3"/>
    </LinearLayout>
    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/btn_question_submit"
        android:text="Submit"
        android:layout_alignParentRight="true"
        android:layout_alignParentEnd="true"
        android:layout_alignParentBottom="true"
        android:background="@color/colorButton"
        android:layout_margin="4dp"
        android:textColor="#ffffff"/>
    <EditText
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/et_question_code"
        android:hint="Code"
        android:layout_toStartOf="@id/btn_question_submit"
        android:layout_toLeftOf="@+id/btn_question_submit"
        android:singleLine="true"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true"
        android:layout_alignParentBottom="true"
        android:layout_margin="4dp"
        />
    <Button
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/btn_question_scan"
        android:background="@color/colorButton"
        android:textColor="#ffffff"
        android:layout_margin="4dp"
        android:text="Scan QR code"
        android:layout_above="@id/et_question_code"/>

    <ImageView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_above="@id/btn_question_scan"
        android:id="@+id/iv_question"
        android:layout_below="@id/ll_question_timers"/>
</RelativeLayout>
